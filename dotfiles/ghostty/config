# Ghostty configuration
# Viwe available config with default values:
#   `ghostty +show-config --default --docs`
# Details:
#   https://ghostty.org/docs/config

macos-option-as-alt = true

# # My default font config.
# font-family = "IntoneMono NF"
# font-family = "HackGen"
# font-size = 14
# # Disable ligatures.
# font-feature = -calt
# font-feature = -dlig
# font-feature = -liga

background = #2d353b
foreground = #d3c6aa

# Override some default key bindings related to Enter and let them just emits Enter.
# By pressing Enter immediately after some keys using Ctrl or Super,
# I sometimes accidentally input these bindings but I always just want to input Enter.
keybind = ctrl+enter=text:\r
keybind = super+enter=text:\r

config-file = ?local
