#compdef npmb

# http://blog.glidenote.com/blog/2012/05/03/zsh-completion/

function _npmb() {
    if (( CURRENT == 2 )); then
        _files -W "$(pwd)/node_modules/.bin"
    else
        _files
    fi
}

_npmb "$@"
