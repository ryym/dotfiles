# vim:set filetype=udev:

# Enable to run Xremap without sudo by:
# 1. Add you to the input group: `sudo gpasswd -a <user> input`
# 2. Register the following udev rule.
# https://github.com/xremap/xremap?tab=readme-ov-file#running-xremap-without-sudo
KERNEL=="uinput", GROUP="input", TAG+="uaccess"

# If the above rule does not work, you may have to load the uinput kernel module explicitly.
# `echo uinput > /etc/modules-load.d/uinput.conf`
# https://github.com/chrippa/ds4drv/issues/93#issuecomment-265300511
