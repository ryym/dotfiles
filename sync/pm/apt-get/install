#!/usr/bin/env bash

install() {
    echo "Install $1"
    sudo apt-get install -y "$@"
}

sudo apt-get update

install git
install tmux
install zsh

# For Ruby compiling
install libssl-dev
install libreadline-dev
install zlib1g-dev
